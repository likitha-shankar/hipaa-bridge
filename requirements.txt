fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0

# Optional NER for person/location names (regex fallback works without it):
#   pip install spacy && python -m spacy download en_core_web_sm
